中期作业六:

课堂作业，现场检查

（1）在工程UDPChat_CMD的基础上，写一个简单的TicTacToe对弈程序，实现服务器端与客户端的对弈。

例如，可设定客户端为X方，服务器端为O方，客户端输入棋子位置（0-8），先走棋，然后服务器端输入（可下）棋子位置，双方交替下棋，直到棋局结束或有一方输入q退出对弈为止。在对弈过程中，服务器端与客户端都要显示棋局状态。

额外功能（可选）包括：判断胜负、判断禁手等。